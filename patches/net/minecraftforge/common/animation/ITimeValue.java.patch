--- ../src-base/minecraft/net/minecraftforge/common/animation/ITimeValue.java
+++ ../src-work/minecraft/net/minecraftforge/common/animation/ITimeValue.java
@@ -25,7 +25,6 @@
  * Simplest example is the input time itself.
  * Unity calls them Parameters, Unreal calls them Variables.
  */
-public interface ITimeValue
-{
+public interface ITimeValue {
     float apply(float input);
 }
