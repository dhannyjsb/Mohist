++++ REJECTED PATCH 14
             boolean moddedClientAllowed = true;
             if (!Strings.isNullOrEmpty(serverDescription))
             {
-                moddedClientAllowed = !serverDescription.endsWith(":NOFML搂r");
+                moddedClientAllowed = !serverDescription.endsWith(":NOFML锟斤拷r");
             }
             serverDataTag.put(data, new ExtendedServerListData("VANILLA", false, ImmutableMap.of(), !moddedClientAllowed));
         }
++++ END PATCH
++++ REJECTED PATCH 15
 
     public String fixDescription(String description)
     {
-        return description.endsWith(":NOFML搂r") ? description.substring(0, description.length() - 8)+"搂r" : description;
+        return description.endsWith(":NOFML锟斤拷r") ? description.substring(0, description.length() - 8)+"锟斤拷r" : description;
     }
 
     public void connectToServerAtStartup(String host, int port)
++++ END PATCH
